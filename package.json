{
  "name": "feedback-system",
  "version": "1.0.0",
  "description": "Sistema de Feedback para Equipes",
  "main": "backend/index.js",
  "scripts": {
    "install:backend": "npm --prefix backend install --omit=dev",
    "install:frontend": "npm --prefix frontend install --include=dev",
    "build:frontend": "npm run install:frontend && npm --prefix frontend run build",
    "build": "npm run install:backend && npm run build:frontend",
    "start": "node backend/index.js",
    "postinstall": "npm run build",
    "install-all": "npm install && npm run install:backend && npm run install:frontend"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
